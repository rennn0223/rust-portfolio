[build]
target = "index.html"
# 這是確保 GitHub Pages 能找到 .wasm 的關鍵
public_url = "/rust-portfolio/"

[serve]
port = 8080

[[hooks]]
stage = "post_build"
command = "sh"
command_arguments = ["-c", "cp -r assets dist/"]
